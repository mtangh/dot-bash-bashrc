# ${HOME}/.profile
# User local .profile for sh(1) and compatible shells.

# This describes common configuration for the Bourne shell (sh(1))
# and Bourne compatible shells (bash(1), ksh(1), ash(1), ...).

# Prompt setting (default)
PS1='\u@\h \W\$ '


# End
return 0
